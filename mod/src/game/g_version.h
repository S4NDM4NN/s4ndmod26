#pragma once
/* Build version — overwritten at compile time by the Dockerfile.
   Local / non-CI builds fall back to "dev". */
#ifndef MOD_BUILD_VERSION
#define MOD_BUILD_VERSION "S4NDMoD dev"
#endif
